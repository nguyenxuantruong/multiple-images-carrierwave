<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @gallery.name %>
</p>
<p>
	<% @gallery_attachments.each do |g| %>
		<%= image_tag g.image_url(:thumb) %>
		<%= link_to "Edit Attachment", edit_gallery_attachment_path(g) %>
	<% end %>
</p>

<%= link_to 'Edit', edit_gallery_path(@gallery) %> |
<%= link_to 'Back', galleries_path %>
